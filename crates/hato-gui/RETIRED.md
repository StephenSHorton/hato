# hato-gui — retired

The Tauri desktop app is **no longer built or released**.

User-facing transfer lives in **[suzuri](https://github.com/StephenSHorton/suzuri)** (command palette + CLI wrapping this engine).

This crate remains in the tree only as historical reference. It is **excluded** from the Cargo workspace (`Cargo.toml` `exclude`).
