{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + dialog permissions for the Hato GUI window. Custom app commands (start_send, start_receive, …) are permitted implicitly.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
